---

- name: Start/Restart Minio
  ansible.builtin.command:
    cmd: "docker compose -f {{ docker_compose_minio_file_name }} up -d --force-recreate"
    chdir: "{{ docker_compose_services_directory }}"
  when: docker_compose_minio_start
