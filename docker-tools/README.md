# docker-tools

Docker images for development and CI/CD tooling. These are not service images — they are utility containers used to run infrastructure tooling without installing dependencies locally.

## Available tools

| Tool | Image | Description |
|------|-------|-------------|
| `ansible-ubuntu` | `dmitryshomrin/ansible` | Ansible runtime for running playbooks locally or in CI |
